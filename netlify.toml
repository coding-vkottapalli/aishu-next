[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  # Next.js 16 requires Node 20+
  NODE_VERSION = "20"
  # @testing-library/react@13 peer-deps don't allow React 19; relax resolution
  NPM_FLAGS = "--legacy-peer-deps"
